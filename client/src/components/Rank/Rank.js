import React from 'react'

const Rank = ({name}) => {
    return (
        <div>
            <div className="white f3">
                {`${name}, welcome to use our Smart Brain!`}
            </div>
        </div>
    );
}

export default Rank;